#include <iostream>
#include "../include/DNSTypes.h"
using namespace std;

int main()
{
    dns::RecordType type = dns::RecordType::A;
    cout << "DNS SUCCESSFULLY COMPILED!";
    system("pause");

    return 0;
}